[build-system]
requires      = ["maturin>=1.0"]
build-backend = "maturin"

[project]
name        = "proof-sdk"
version     = "0.1.0"
description = "PROOF — financial logic, verified pure"
requires-python = ">=3.10"

[tool.maturin]
python-source = "python"
module-name   = "proof._proof_core"

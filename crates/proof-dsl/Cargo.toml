[package]
name    = "proof-dsl"
version = "0.1.0"
edition = "2021"
description = "PROOF specification language — lexer, parser, and AST"

[dependencies]
serde        = { workspace = true }
serde_json   = { workspace = true }
thiserror    = { workspace = true }
anyhow       = { workspace = true }
rust_decimal = { workspace = true }
chrono       = { workspace = true }
